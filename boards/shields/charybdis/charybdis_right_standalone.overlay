/* boards/shields/charybdis/charybdis_right_standalone.overlay */

/ {
    chosen {
        zmk,kscan = &kscan0;
        zmk,physical-layout = &charybdis_6col_layout;
    };
};

/* 오른쪽 키보드는 왼쪽과 핀 배열이 대칭이므로 col-gpios 순서를 뒤집거나 조정할 필요가 있을 수 있음.
   일단 위에서 정의한 kscan0를 그대로 사용. */
